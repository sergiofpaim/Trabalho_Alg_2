#include "../include/data.h"

#include <stdlib.h>

/* ============================================================
   Seção: Estruturas para serialização/desserialização
   ============================================================ */

struct Usuarios usuarios = {0, NULL};
struct Jogos jogos = {0, NULL};
struct Recordes recordes = {0, NULL};